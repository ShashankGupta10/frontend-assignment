type SidebarPropsType = {
    title: string
    icon: JSX.Element
    section: string
};

type PlayListType = {
    title: string
    artist: string
    image: string
    time: string
    audioURL: string | null
};